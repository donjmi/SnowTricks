{% extends 'base.html.twig' %}

{% block title 'Gestion des figures' %}
 
{% block body %}
    <div class="container-fluid">
        <h1 class="my-5 text-center">Gérer les figures</h1>
        <div class="text-center mb-2">
            <a href="" class="btn btn-success text-white mb-3"><i class="fas fa-plus"></i>  Nouvelle figure</a>
        </div>
    </div>
    <div class="container mt-3">
        <table class="table table-striped">
            <thead>
                <th>Nom</th>
                <th>Categorie</th>
                <th>Actions</th>
            </thead>
            <tbody>
	        {% for trick in tricks %}
            <tr>
                <td>{{ trick.name }}</td>
                <td>{{ trick.categorie.label }}</td>
                <td>
                <a href="{{ path('admin.trick.edit', {id: trick.id}) }}" class="btn btn-warning"><i class="fas fa-edit"></i> Editer</a>
                <a href="{{ path('admin.trick.edit', {id: trick.id}) }}" class="btn btn-danger"><i class="fas fa-trash"></i> Supprimer</a>
                </td>
            </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>

{% endblock %}
